# Инструкции по деплою в GitHub

Репозиторий: https://github.com/npopova90/feature_bot

## ⚡ Быстрый способ (GitHub Desktop)

1. **Скачайте и установите GitHub Desktop:**
   - https://desktop.github.com/
   - Войдите в свой GitHub аккаунт

2. **Откройте GitHub Desktop:**
   - File → Add Local Repository
   - Выберите папку: `C:\Users\arehis\Desktop\Рабочие доки\Фичебот`
   - Нажмите "Add repository"

3. **Опубликуйте репозиторий:**
   - Нажмите "Publish repository" (кнопка вверху)
   - Убедитесь, что выбран `npopova90/feature_bot`
   - Снимите галочку "Keep this code private" (если нужен публичный репозиторий)
   - Нажмите "Publish repository"

✅ **Готово!** Код будет загружен в GitHub.

---

## 🔧 Способ через командную строку (Git)

### Шаг 1: Установите Git

Скачайте и установите Git для Windows:
- https://git-scm.com/download/win
- Установите с настройками по умолчанию
- **Важно:** Перезапустите терминал после установки

### Шаг 2: Выполните команды

Откройте **PowerShell** или **Git Bash** в папке проекта и выполните:

```bash
# Инициализация репозитория
git init

# Добавление remote
git remote add origin https://github.com/npopova90/feature_bot.git

# Добавление всех файлов
git add .

# Создание коммита
git commit -m "Initial commit: Telegram bot for feature test summaries with GPT-5.2"

# Настройка ветки main
git branch -M main

# Push в GitHub
git push -u origin main
```

**Примечание:** При первом push вам потребуется авторизация GitHub:
- Либо введите логин/пароль
- Либо используйте Personal Access Token (рекомендуется)
  - Создайте токен: https://github.com/settings/tokens
  - Используйте его вместо пароля

---

## 📋 Альтернатива: Загрузка через веб-интерфейс

Если Git не установлен, можно загрузить файлы через веб:

1. Откройте https://github.com/npopova90/feature_bot
2. Нажмите "uploading an existing file"
3. Перетащите все файлы из папки проекта
4. **НЕ загружайте:**
   - `node_modules/` (папка)
   - `.env` (файл)
   - `dist/` (папка)
   - `.git/` (если есть)
5. Нажмите "Commit changes"

---

## ✅ Проверка после push

После успешного push откройте:
https://github.com/npopova90/feature_bot

Убедитесь, что все файлы загружены:
- ✅ `src/` - исходный код
- ✅ `package.json` - зависимости
- ✅ `README.md` - документация
- ✅ `Dockerfile` - для деплоя
- ✅ `prompts/` - промпты
- ✅ `scripts/` - скрипты
- ❌ `.env` - НЕ должен быть в репозитории (уже в .gitignore)

---

## 🚀 После push: Настройка Railway

После того как код будет в GitHub:

1. Откройте https://railway.app
2. Создайте новый проект
3. Подключите GitHub репозиторий `npopova90/feature_bot`
4. Настройте переменные окружения:
   - `TELEGRAM_BOT_TOKEN`
   - `OPENAI_API_KEY`
   - `OPENAI_MODEL=gpt-5.2`
   - `EXCEL_PATH=./data/feature_tests.xlsx`
   - `BOT_MODE=webhook`
   - `WEBHOOK_URL` (Railway предоставит)
   - `PORT=3000`
5. Railway автоматически соберет и задеплоит проект

---

## ❓ Проблемы?

**Ошибка: "repository not found"**
- Проверьте, что репозиторий существует: https://github.com/npopova90/feature_bot
- Убедитесь, что вы авторизованы в GitHub

**Ошибка: "authentication failed"**
- Используйте Personal Access Token вместо пароля
- Создайте токен: https://github.com/settings/tokens (scope: repo)

**Ошибка: "remote origin already exists"**
- Выполните: `git remote remove origin`
- Затем: `git remote add origin https://github.com/npopova90/feature_bot.git`
